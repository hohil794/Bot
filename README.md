# 🏮 Telegram-бот "Оданна" - Хозяин Небесной Гостиницы

Telegram-бот на основе персонажа **Оданны** из аниме "Повар небесной гостиницы", интегрированный с нейросетью для генерации ответов в соответствии с характером персонажа.

## ✨ Особенности

- 🎭 **Аутентичный характер** - точное воспроизведение личности Оданны
- 🧠 **Интеграция с ИИ** - использует DialoGPT для генерации ответов
- 💾 **Система памяти** - хранение всех диалогов с возможностью "забыть/вспомнить"
- 📊 **Динамическая эмпатия** - уровень отзывчивости изменяется в зависимости от диалога
- 💬 **Мультичат** - поддержка множественных диалогов с каждым пользователем
- 🎯 **Анализ эмоций** - распознавание эмоционального состояния собеседника

## 🏗️ Архитектура

```
🏮 OdannaBot/
├── 📄 odanna_bot.py         # Основной файл бота (все в одном)
├── 📄 requirements.txt      # Зависимости Python
├── 📄 README.md            # Документация
├── 🗃️ odanna_bot.db        # База данных SQLite (создается автоматически)
└── 📁 logs/                # Логи (создается автоматически)
```

## 🚀 Быстрый старт

### 1. Создание Telegram-бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен бота

### 2. Установка зависимостей

```bash
# Клонирование репозитория
git clone <your-repo-url>
cd odanna-bot

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 3. Настройка

```bash
# Установка токена бота
export BOT_TOKEN="your_bot_token_here"

# Или создайте файл .env
echo "BOT_TOKEN=your_bot_token_here" > .env
```

### 4. Запуск

```bash
python odanna_bot.py
```

## 🔧 Подробная конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram-бота | Обязательно |
| `DB_PATH` | Путь к файлу базы данных | `odanna_bot.db` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |

### Настройка базы данных

База данных SQLite создается автоматически при первом запуске. Схема включает:

- **users** - информация о пользователях
- **chats** - настройки чатов и сценарии
- **messages** - история сообщений с анализом эмоций

## 🌐 Деплой на бесплатном хостинге

### Вариант 1: Render.com

1. **Подготовка репозитория:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

2. **Создание аккаунта на [Render.com](https://render.com)**

3. **Создание Web Service:**
   - Connect Repository → выберите ваш репозиторий
   - Name: `odanna-telegram-bot`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python odanna_bot.py`

4. **Настройка переменных окружения:**
   - В разделе Environment добавьте `BOT_TOKEN`

5. **Deploy** - Render автоматически развернет бота

### Вариант 2: Fly.io

1. **Установка Fly CLI:**
   ```bash
   # Linux/Mac
   curl -L https://fly.io/install.sh | sh
   
   # Windows
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Создание fly.toml:**
   ```toml
   app = "odanna-bot"
   kill_signal = "SIGINT"
   kill_timeout = 5
   processes = []

   [env]
     PORT = "8080"

   [experimental]
     auto_rollback = true

   [[services]]
     http_checks = []
     internal_port = 8080
     processes = ["app"]
     protocol = "tcp"
     script_checks = []
   ```

3. **Деплой:**
   ```bash
   fly auth login
   fly launch
   fly secrets set BOT_TOKEN=your_bot_token_here
   fly deploy
   ```

### Вариант 3: Heroku

1. **Создание Procfile:**
   ```
   worker: python odanna_bot.py
   ```

2. **Деплой:**
   ```bash
   heroku create odanna-bot
   heroku config:set BOT_TOKEN=your_bot_token_here
   git push heroku main
   heroku ps:scale worker=1
   ```

## 🎭 Использование бота

### Основные команды

- `/start` - Запуск бота и главное меню
- `Забудь [текст]` - Пометить сообщение как забытое

### Интерфейс

1. **Главное меню:**
   - 🏨 Создать новый чат
   - 📋 Мои чаты  
   - ⚙️ Настройки

2. **Создание чата:**
   - 🎭 С настройками сценария
   - 🏮 Стандартный (Небесная Гостиница)

3. **Управление чатами:**
   - Просмотр истории
   - Удаление чата
   - Переименование

### Система эмпатии

Бот автоматически регулирует уровень эмпатии (35-85%):

- **35%** - Начальный уровень (сдержанность, ирония)
- **50%** - Средний уровень (вежливость, внимание)
- **70%+** - Высокий уровень (забота, поддержка)

Факторы влияния:
- Количество сообщений в диалоге
- Эмоциональное состояние пользователя
- Контекст беседы

## 🛠️ Разработка и кастомизация

### Структура классов

```python
DatabaseManager     # Управление SQLite базой
AIManager          # Интеграция с нейросетью
OdannaBot         # Основная логика бота
```

### Добавление новых сценариев

1. Модифицируйте `ODANNA_SYSTEM_PROMPT`
2. Добавьте логику в `_handle_create_chat`
3. Обновите систему анализа эмоций в `analyze_emotion`

### Настройка ответов

Функция `_fallback_response` содержит заготовленные ответы для разных уровней эмпатии. Можно добавить новые варианты или изменить существующие.

## 📊 Мониторинг и логирование

Бот ведет подробные логи:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
```

Логируются:
- Запуск/остановка бота
- Ошибки загрузки модели
- Ошибки генерации ответов
- Действия пользователей

## 🔒 Безопасность

- Токен бота хранится в переменных окружения
- База данных локальная (SQLite)
- Логирование не содержит персональных данных
- Обработка ошибок предотвращает утечку информации

## 🐛 Устранение неполадок

### Проблема: Бот не отвечает

**Решения:**
1. Проверьте корректность токена бота
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Проблема: Модель не загружается

**Решения:**
1. Проверьте доступ к интернету
2. Убедитесь в наличии свободного места (модель ~1GB)
3. При нехватке памяти будут использоваться fallback-ответы

### Проблема: База данных недоступна

**Решения:**
1. Проверьте права доступа к директории
2. Убедитесь в наличии свободного места
3. При необходимости удалите файл БД для пересоздания

## 📈 Производительность

### Рекомендуемые характеристики сервера

- **RAM**: минимум 2GB (рекомендуется 4GB)
- **CPU**: 1 ядро (рекомендуется 2)
- **Место**: 5GB (включая модель ИИ)
- **Интернет**: стабильное соединение

### Оптимизация

- Модель загружается один раз при старте
- База данных оптимизирована индексами
- Параллельная обработка до 100 пользователей

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

### Гайдлайны

- Соблюдайте характер Оданны
- Добавляйте комментарии к коду
- Тестируйте новые функции
- Обновляйте документацию

## 📝 Лицензия

MIT License - свободное использование с указанием авторства.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте этот README
2. Изучите логи бота
3. Создайте Issue в репозитории
4. Опишите проблему подробно

---

*Создано с ❤️ для фанатов аниме "Повар небесной гостиницы"*

🏮 **"В Небесной Гостинице каждый гость найдет то, что ищет..."** - Оданна
